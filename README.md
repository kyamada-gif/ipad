# IPアドレスの計算（試作2）

教材PDF「IPアドレスの計算方法」を、そのままアプリにしたもの。

```bash
cd ipcalc2 && open index.html
# うまく開けないときは
cd ipcalc2 && python3 -m http.server 8778    # → http://localhost:8778
```

サーバーもビルドも要りません。React は `vendor/` に同梱しています。

---

## 1. 背骨 ── 教材の手順に、暗記を足したもの

**軸は教材（PDF）です。**やり方は教材の2進数の手順そのまま。
そこに **「よく出るものを丸暗記させる」** という考えを足してあります。
教材に無い近道の解き方（256 から引いて刻み幅を出す、など）は**入れません**。

思想を1行で書くとこうです。

> **画面も・覚える中身も・進み方も、「いま思い出すこと」に効くものだけを、最小の形で置く。**

### どのステージも同じ道すじ

```
札を押す →「練習をする」→ 説明の1枚 ─┬→「練習をする」 盤で5問
                                      └→「テストをする」 表なしで10問（仕上げは15問）
```

説明の1枚は、10ステージすべて同じ段です（くわしくは 12章）。

```
前のステージから → ┃解き方 → （図で見ると）→ 見本 → やってみる
```

**仕上げ（ステージ10）だけは練習を置きません。**新しいやり方が無いので、
札を押す →「はじめる」→ 説明の1枚 →「テストをする」の一本道です。

### 全体で1つの決まり

- **正解するまで、次へ進まない。**練習でもテストでも同じ。
  点になるのは**その問題の最初の答えだけ**（やり直しで全員が合格にならないように）
- **合っても外しても、根拠を小さく1行**出す
- **押せるものは4つ以下**
- 覚えさせるのは、**本番の答えに出てくる数だけ**

### 見た目の決まり（Apple の Human Interface Guidelines より）

- 文字は **7段だけ**（11 / 13 / 15 / 17 / 22 / 34 / 44）
- 色は **決めた32色だけ**。意味も1つに固定 ──
  青＝いま選んでいるもの／緑＝進む・できた／赤＝ちがう／黄＝区切りの線／灰＝まだ
- **色だけに頼らない。**選んでいるものは色に加えて太字にもする
- 小さい文字は **4.5:1 以上**のコントラスト。押すところは **44px** 以上

段や色を増やすと **`node build.js` が止まります。**画面ごとに少しずつずれていくのを防ぐためです。

---

## 2. 覚えることは3つの状態だけ

```
○ まだ            まだ5問中4問できていない
● 表を見てできた   5問中4問。次のステージが開く
★ 表なしでできた   もう一度やって、また4問。本番と同じ形
```

- **練習は5問。**前半はやさしい数、後半は本番どおりの出方
- **テストは10問**（仕上げだけ15問）。合格は9割
- 間違えたときだけ、その場で手順が全部出ます
- **そのステージが初めてのときだけ**、教材の見本が自動で出ます（2回目からは「やり方を見る」）

**正解のあと**は、待たなくても画面を押せばすぐ次へ行きます（そのままでも自動で進みます）。
押した指がそのまま触れて豆知識を飛ばさないよう、0.3秒だけ効かないようにしてあります。

**練習で間違えたときは、同じ問題をもう一度やります。**手順は出したまま残るので、
それを見ながらなぞって、最後は「できた」で終われます。
点になるのは**最初の答えだけ**です（そうしないと、やり直せば誰でも毎回合格になり、
「5問中4問」に意味が無くなります）。2回やり直しても合わないときは「次へ」も出します。
**テストはやり直しません。**本番と同じ、一度きりの形にしています。

---

## 3. テスト（表なし）── 一度できたステージをもう一度やると

教材はこう書いています。

> この表は何度も使うことになるので、**自分で書けるようにしておくこと。**

だから **テストでは、桁の重みの表を出しません。**問いと、自分で計算する場所だけです。

札を押すと「練習をする」と「テストをする」が出ます。
**テストは、練習でできてから**開きます（手順を知らないまま4択をやっても、
4回に1回当たるだけで記録が汚れるため）。

| | 練習 | テスト |
|---|---|---|
| 盤 | 桁の重みの表を出す | 出さない。自分で計算する |
| 問題数 | 5問（4問で ●） | 10問（9問で 🏅）／仕上げは15問（14問） |
| 札の合図 | ○ まだ ／ ● 練習ができた | 🏅 バッジ |

鍵のかかったステージは開きません。順番はひとつだけです。

```
今回は表なし。自分で計算します

2進数  00111100

┌ 計算 ────────────┐
│  32 ＋ 16 ＋ 8 ＋ 4  │
│                 60  │
│  7  8  9  ⌫         │
│  4  5  6  ＋        │
│  1  2  3  −         │
│  0     00  C        │
└────────────────┘
[ 60 で決定 ]
```

**計算するところは ＋ と − だけ**にしました。教材の手順に ×÷ は一度も出てきませんし、
本番に無い道具に慣れても仕方がないためです。
**どの重みを足す（引く）かを決めるのが人、足し引きの答えを出すのが機械。**
表を使う回とまったく同じ線引きです。

### 答えの入れ方は、ステージによって変えています

| ステージ | 入れ方 | なぜ |
|---|---|---|
| 1 | 計算した数がそのまま答え | 3桁までなので軽い |
| 2 | **重みの書いていない空の8マス** | 打ち間違いが起きない。どの桁がいくつかは自分の頭から出す |
| 3〜7 | **選ぶ（4択）** | **本番の CCNA も選択式。**アドレス2つを全部打たせると1問が重すぎる |

まちがいの選択肢は**よくある間違いから作っています。**でたらめな値は入れません。

| ステージ | 入れてあるまちがい |
|---|---|
| 3 | 使える範囲と取りちがえた／先頭と末尾が逆／線を1つずらした |
| 4 | ±1 を忘れた／片方だけ忘れた／線をずらした |
| 5 | **「2を足す」を忘れた**／1つ隣の長さ |
| 6 | **教材どおり「2を足した」**／1つ隣の長さ／基準のまま |
| 7 | 1ビット足りない・多い／まとめる前のまま／次のまとまり |

### 速さの記録は、表ありと表なしで別に持ちます

表なしは必ず遅くなるので、同じ物差しで比べても意味がないためです。

---

## 4. 教材と1か所だけ変えたところ

**6ステージ「必要なサブネット数からマスク」。**教材は必要な数に **2 を足して**いますが、
これはサブネットゼロが使えなかった時代の数え方です。
いまの CCNA では足しません。**このアプリは足しません。**

境界で答えがずれます（`/16` から延ばす場合）。

| 必要な数 | いまの試験 | 教材どおり | |
|---|---|---|---|
| 3個 | /18 | /19 | **ずれる** |
| 10個 | /20 | /20 | 同じ |
| 16個 | /20 | /21 | **ずれる** |
| 19個 | /21 | /21 | 同じ |
| 32個 | /21 | /22 | **ずれる** |

**5ステージの「台数に 2 を足す」は、いまも正しい**ので、そのままです
（先頭のアドレスと末尾のアドレスは、機器に付けられないため）。

**このずれは、画面には出しません。**前は正解した直後に電球つきで
「教材では 2 を足しているが、それは昔の決まり」と1行出していましたが、消しました。
覚えたばかりの手が、そこで揺れます。**画面には、正しいやり方だけを載せます。**
教材との違いは、この README に書いてあれば足ります。

---

## 5. 練習問題は、その場で作ります

教材の練習問題22問をそのまま出すことはしません。**数字を変えて無限に作ります。**
固定だと「252 = 11111100」を丸暗記してしまい、初めて見る数字で解けなくなるためです。

**やり方（見本）だけは教材の数字のまま**にしてあります。
読んだ直後に開いて「これだ」とつながるようにするためです。

出す長さは、原本4冊・全1650問に実際に出てきた回数に寄せています
（`/24` が232回でいちばん多い。詳しくは `out/full/IP計算アプリ_土台の洗い出し.md`）。

---

## 6. 答えが合っている根拠

### ① 生成器の答え

**Python の `ipaddress`** で作り直して突き合わせています。
同じ実装で検算しても同じ間違いをするだけなので、わざと別の言語・別の実装で照合しています。

```bash
node selftest.js 400 > /tmp/q.jsonl && python3 selftest.py /tmp/q.jsonl
# → 検算した問題  4000 問（10 ステージ × 400 問）
#    すべて一致。
```

### ② 画面を本当に描いて触る

ブラウザを開かずに、**アプリを描画して押します**（jsdom を使用）。7本に分かれています。

```bash
node .check/loop.js      # 一周する道すじ（鍵 → 練習で ● → 次が開く → テストで 🏅 → 外すと ✕）
                         #   ＋ トップ画面の「変換を楽にするコツ」が押すと開くか
node .check/smoke.js     # 10ステージの盤が描けるか ＋ 段の骨組みが10ステージで同じか
node .check/split5.js    # ステージ5・6を12問、①〜⑤の順に解いて正解になるか
node .check/steptest.js  # ステージ5のテスト（手順テスト）が通るか
node .check/table.js     # ステージ1の「表を8つともうめる」が通るか
node .check/final.js     # 仕上げのテスト（15問・8つの型・合格ライン）
node .check/masklen.js   # ステージ4の②：答えの /◯ が、1 の数の足し算から出ているか
```

**確かめられないのは、見た目の大きさとはみ出しだけ**です（絵を描かない仕組みのため）。
そこはブラウザを幅375pxにして目で見ています。

### ③ 盤の答え

**「正しく操作したら、その問題の答えになるか」**も機械で確かめています。

```bash
node build.js
# → 生成器の検査 OK（10 ステージ × 200 問）
#    盤の検査 OK（正しく操作すれば答えになる：1800 通り）
```

これができるのは、**画面と検算が同じ関数を使っている**からです
（`gen.js` の `splitOut` / `pickOut` / `stackOut`）。
画面側にも同じ計算を書くと、必ずどちらかがずれます。

この検査は「答えが合っているか」だけでなく、**その答えが盤の上で出せるか**も見ます。
実際、ステージ9で「/24 のように区切りがオクテットの境目にくる問題」が出たとき、
盤には8つの桁しか並んでいないので線が引けず、**答えようがない問題**になっていました。
見るオクテットを1つ手前にずらして直し、線が1〜8ビット目の外に出たら止まるようにしました。

---

## 7. 保存しているもの

ブラウザの `localStorage` に `ipcalc2-progress` の1つだけ。

```js
{ "S3": { seen: 12, correct: 10, lit: true, solo: false,
          bestMs: 18200, testBestMs: 41500, lastMs: 19100 } }
```

**導けるものは保存しません。**開いているかどうかは `need` と `lit` から毎回導いています。

---

## 8. ファイル

| ファイル | 中身 |
|---|---|
| `gen.js` | **問題を作るところ**と、**盤の計算**。画面に依存しない |
| `app.jsx` | 画面。5つの盤（写す／減らす／線を引く／右から押す／縦に並べる）＋ テスト（表なし） |
| `app.js` | `app.jsx` から生成。**手で編集しない** |
| `build.js` | ビルド ＋ 生成器の検査 ＋ **盤の検査** ＋ 言葉の見張り |
| `selftest.js` / `selftest.py` | 生成器の答えを Python で照合 |
| `index.html` / `vendor/` | 入口と React 本体 |

`app.jsx` を直したら `node build.js` で作り直します。

---

## 9. 前のアプリ（`ipcalc/`）との関係

**引き継いだもの**：Python での全件照合／札の状態と1本道の進み方／1セッション5問／
前半はやさしく／盤に答えを刷り込まない／秒数を途中で見せない／
Apple の Human Interface Guidelines の数値（押すところ44px、文字は最小11px、コントラスト4.5:1）。

**変えたもの**：骨格を「16の操作」から **「盤1つ＋10ステージ」** に。
2ステージの向きを足し算から **引き算** に（教材と同じ向き）。
3ステージの入力を「幅＋つまみ」から **縦の線** に。
補助なしの回を「4択だけ」から **「表なし＋自分で計算するところ」** に。

**2つを並べたままにはしない予定です。**しばらく両方触って、手が伸びた方に寄せてください。

---

## 10. 直したところ（画面の確認で見つかったもの）

Fable 5 に判断してもらって直しました。

| 見つかったこと | 直し方 |
|---|---|
| **やさしい問題に `/12` が集中していた** | 3ステージ・4ステージのやさしい問題は `/17` 以上に限る。<br>`/12` は原本で1回しか出ないのに、候補が `/12 /20 /28` の3つだけで<br>3回に1回出ていた。いまは `/20` `/28` の2つ |
| `32768` が幅375pxでマスからはみ出す | 5桁のときだけ文字を11pxに（最小の決まりの範囲内） |
| 1ステージ・2ステージで、何も押さずに決定できた | 1つ以上押すまで押せない。<br>2ステージを「残りが0まで押せない」にはしない（合否を機械が教えてしまうため） |
| 7ステージで、まとめる対象が2つだけのことがあった | やさしいときは必ず4つ。後半は 2・4・8 を混ぜる |
| 1回目から「前より速い」と出ていた | 1回目は「いまの速さ」。2回目から「前より速い」 |
| 教材の見本にたどり着きにくい | **そのステージが初めてのときだけ自動で開く。**<br>ボタンも画面も増やさない（2回目からは小さいボタンのまま） |
| **答え合わせが画面の外に落ちていた** | 判定は「これで決定」より下に出ていたので、スマホでは見えなかった。<br>いまは**画面の下に貼り付く帯**。決定を押した直後、目も指もそこにある。<br>✓✕ の形と色の両方を出す（色の見え方は人によって違う） |
| 間違えたとき、正しい答えが手順に埋もれていた | 帯に**「答えは ○○」**を大きく出す。4択では正解が緑、押した外れが赤 |
| 7ステージで、答えようのない問題が出ていた | `/24` のように区切りがちょうど境目にくると、8つ並んだ桁の上に線が引けなかった。<br>見るところを1つ手前にずらして直した（約3.5%の問題が該当）。<br>線が8桁の外に出たら `build.js` が止まる |
| 「区切りのあるオクテット」が通じない | **マスクそのものを点ごと並べて、その数字を直接押す**形に。<br>上の材料欄は出さない（盤の中の数と別物に見えるため） |
| **255 でない数だけ明るくしていた** | 「255 じゃないところを見つける」という判断を機械が先にやっていた。<br>「光らせる手引きは入れない」と決めた方針に自分で反していたので削除 |
| 1ステージで「この2進数」がどれを指すか分からない | 問題の数と押すところが同じ見た目で縦に並んでいた。<br>左に名札、**枠を持つ／持たない**で見た目を分けた（列はそろえたまま） |

### 説明の1枚を、10ステージで同じ骨組みに揃えた

同じ「ここは1つのかたまりです」を、**7通りの見た目で言っていました。**
灰の箱（前のステージから）／赤い縦線（解き方）／黄色いベタ塗り（まずは1問…）／
青い縦線（盤の中）／名札も枠も無いただの文章（ステージ4の3段落）／上に細い罫（見本）／
同じく上に細い罫（チュートリアル本体）。
そのうえ、**いちばん目立つ黄色いベタ塗りが、いちばんどうでもいい一言**に使われていました。

| 見つかったこと | 直し方 |
|---|---|
| **ステージごとに段の数と順番が違った** | どのステージも同じ4つの名札に。<br>`前のステージから` → `解き方` → （`図で見ると`）→ `見本` → `やってみる`。<br>名札の見た目は1種類だけ（13px 灰・下地なし・枠なし・縦線なし） |
| **目立つ帯が画面に4つあった** | 赤い縦線の「解き方」1つだけに。<br>ここには名札を付けない（太字の見出しが名札そのもの）。<br>黄色いベタ塗りは、**形が変わる予告**（練習の「ここからは自分で選びます」）専用に戻した |
| ステージ4・5だけ、図と文章が画面ファイルに直書きだった | `gen.js` の `FIGURE` に移した。`WAY` `LINK` `EXAMPLES` と同じ形。<br>名札の無い文章が3つ続く、という段がこれで消えた |
| **「点で区切られた4つのサブネット」** | 点で区切られた4つは**サブネットではなくオクテット**。文の中で自分と矛盾していた。<br>「4つのまとまり」に直した |
| ステージ1だけ「前のステージから」が無かった | `LINK.S0` を足した。最初の画面だけ1段短い、が無くなった |
| ステージ4だけ「まずは1問…」が2回出ていた | 断り書きは「やってみる」の段に1回だけ。①② の見出しだけが2回 |
| ステージ8の「※ 教材との違い」が表の行だった | 「クラスB の出発点は」と同じ顔で並ぶと手順の1つに見える。表の外の注記に出した |
| 下の固定ボタンが、後ろの盤の字と重なっていた | 上に地の色へのぼかしを敷いた。<br>**緑は「いま進む道」1つだけ。**テストは灰の枠にして、どちらが先かを見た目で言う |
| 12行の説明文（`HOW`）が1回も画面に出ていなかった | `WAY` が全10ステージにあるので、`HOW` の枝には永久に来なかった。削除 |

**分けたままにしたもの**：3ステージと4ステージ。盤は同じですが、3ステージはマスクが点表記で渡され、
4ステージは `/22` の形で渡されて「どのオクテットに区切りがあるか」を自分で判断します。
さらに「先頭と末尾は使えない、±1する」が加わるので、自動的にはできません。
ステージ番号が教材の①〜⑦とそのまま対応していることも、そのままにしています。

### 見た目の見張りに、穴が空いていた

「文字は7段だけ・色は決めた色だけ」を `build.js` が見ているつもりでしたが、

```js
/font-size:(\d+)px/     // ← \d+ のあとすぐ px。12.5px には一致しない
```

**小数の大きさは検査そのものが素通り**していました。実際に3つ抜けていました
（`.blocked` 11.5px ／ `.step-t` 12.5px ／ `.step-v` 14.5px）。

書き方で抜けられない形に変えました。

- **文字の大きさと色を書いてよいのは `:root` の中だけ**（`app.jsx` の CSS の先頭）
- `:root` の外に `font-size:◯px` か `#hex` があれば、**ビルドが止まる**
- 表にあるのに使われていない変数があっても止まる（消したのに表だけ残る、を防ぐ）
- 余白（`--s1`〜`--s6`）は、まだ直値が残っている画面があるので**数えて出すだけ**。触った所から寄せていく

### `node build.js` が、約1%の確率で落ちていた

作業中に一度だけ `SF: 向き sum の問題が作れない（ease=1）` で止まりました。
同じコマンドをもう一度打つと通るので、見なかったことにできてしまうやつです。数えたら **約1%** でした。

原因は `GEN.SF` だけ、向きの指定を捨てていたことです。

```js
GEN.SF = (ease) => finalOf(pick(FINAL_KINDS), ease);   // ← goal を受け取っていない
```

`makeQuestion` は「頼まれた向きが出るまで60回やり直す」ので、たいてい間に合います。
ただし8つから引き直すので `(7/8)^61 ≒ 0.03%` で外れます。
検査は1回のビルドで32回頼むため、**1 −(1−0.0003)^32 ≒ 1%** でビルドが落ちていました。

`(ease, want)` を受けるように直しました（`GEN.S0` `GEN.S8` と同じ形）。
3000回試して0回。**問題の中身も、向きを頼まないときの動きも変わりません。**

**止まる検査が時々ウソをつくと、検査そのものを信じなくなります。**
「もう一度打てば通る」が出たら、直す前に必ず回数を数えてください。

### ステージ4の「線」は、考え方そのものが違っていた

前はこう書いていました。

> /28 は「**線**が先頭から 28個目」。サブネットマスクは、ネットワーク部を 1・ホスト部を 0 にして
> 10進数で書いたもの。**同じ線の、2つの書き方**です。

「線」という別のものがあって、`/28` と `255.255.255.240` がその2つの書き方、という説明になっています。
**そうではありません。**

- ネットワーク部とホスト部の**区切り位置を決めるのが、サブネットマスク**
- サブネットマスクの書き方が2つある
  - `255.255.255.240`（10進数）／`11111111.11111111.11111111.11110000`（2進数）
  - **左から 1 が何個つづくか**で書く方法 ＝ **プレフィックス長**（`/28`）
- **表現がちがうだけで、同じもの。**試験ではどちらの形でも聞かれる。**この行き来がこのステージの中身**

図の下の説明を、この順に書き直しました。図の中の黄色い印も
「線 ＝ /28」から「**区切り位置 ＝ 先頭から 28個目**」に変えています。

**まだ「線」と呼んでいるところが残っています**（ステージ5・6・9、`gen.js` に14か所、
`app.jsx` の盤に6か所ほど）。あちらは**8つのマスに実際に引く印**の名前として使っているので、
ステージ4の「定義として使っていた」誤りとは種類が違います。
ただし同じもの（区切り位置）を2つの名前で呼んでいることに変わりはないので、
**「区切り」に揃えるかどうかはオーナーの判断待ち**です。

### 見本の題を「本番の問い」にして、手順に番号を振った

題が `/28 をサブネットマスクに` と**言い切っていません**でした。
本番の問いと同じ言い方（`/28 をサブネットマスクに直してください。`）にそろえると、
**見本＝これから出る問題を1つ解いたもの**だと、読んだ瞬間に分かります。

手順の左に `1. 2. 3.` を振りました。**ステージ9のつづきの行は番号を飛ばします**
（`169 = …` `170 = …` は上の行のつづきで、別の手順ではないため）。

```
見本
/28 をサブネットマスクに直してください。
 1  /28 は 1 が 28 個        左からつめて並べる
 2  8 個で 255 が1つ         28 ÷ 8 = 3 余り 4
 3  255 が3つ                255.255.255.
 4  余り4個は上の桁から       11110000 = 240
 5  答え                     255.255.255.240
```

題を変えていないのは2つだけです。**ステージ1**（覚える表そのもの）と
**ステージ10**（1問の見本ではなく、本番で聞かれる8つの形の一覧）。
どちらも「1つの問題を解いた跡」ではないので、問いの形にすると嘘になります。

**踏んだ落とし穴**：3列目を `auto` にしたら、長い答え
（`11111111.11111111.11111111.11111000 → /29`）が縮まず、**手順のほうが1文字ずつの縦書き**に
なりました。`minmax(0,1fr)` と `minmax(0,1.3fr)` で幅を分け合う形にしています。

### 仕上げのテストを15問に。並び順は毎回まぜる

8つの型は 10問でも一通り出ていました（200回まわして抜けは0）。ただし **並び順が毎回同じ**でした。

```
前：mask → binmask → net → bc → gw → same → sum → hosts → mask → binmask   （いつもこの順）
```

`i % 8` で順ぐりに出していたためです。これだと**問題を読まずに「3問目はネットワークアドレス」と
順番で覚えられます。**8つをまぜてから並べる形（`finalOrder`）にしました。

- **15問**（`FINAL_QN`）。8つを一通り出してから、もう一巡（7つが2回、1つが1回）
- 合格ラインは **14/15**。`CLEAR_N = 9` と直に書いていたのをやめて、
  **問題数の9割から出す**ようにしました（前のままだと 9/15 で合格になるところでした）。
  10問→9問、15問→14問。どちらも「まちがえてよいのは1問」でそろいます

`node .check/final.js` が、実際に最後まで解いて確かめます
（15問あるか／8つの型が出るか／全問正解で 🏅／1問はずしても 🏅／**2問はずすと 🏅 が出ない**）。
最後の1つを見ないと、合格ラインがゆるくなっても気づけません。

### 問いを、全部見直した（練習・テストの両方）

`IPアドレス 192.168.49.134/16` の下に **「サブネットマスクはどれですか。」**。
**その中のどこを、何にするのか**を言っていませんでした。全部の問いを並べて見直したところ、
不明確なものが7つありました。

| ステージ | 前 | いま |
|---|---|---|
| 1 | いくつになりますか。 | **10進数で**いくつですか。 |
| 7 | この台数が入るサブネットマスクはどれですか。 | この台数が入る、**最も小さいサブネットの**サブネットマスクはどれですか。 |
| 8 | このサブネット数が作れるサブネットマスクはどれですか。 | このサブネット数が作れる、**最も小さい**サブネットマスクはどれですか。 |
| 9 | この範囲を1つのアドレスにまとめるとどれですか。 | この**4つ**を1つにまとめると、どのアドレスになりますか。 |
| 10 | サブネットマスクはどれですか。 | **IPアドレスの後ろのプレフィックス長を**、サブネットマスクに直してください。 |
| 10 | **同じものを**、プレフィックス長で書くとどれですか。 | **このサブネットマスクの**、プレフィックス長はいくつですか。 |
| 10 | …アドレスを使います。**どれですか。** | デフォルトゲートウェイに使う、…アドレス**はどれですか**。 |

**ステージ7・8は、言葉だけの問題ではありませんでした。**
「100台が入るサブネットマスク」は `/25` でも `/24` でも `/23` でも当てはまります。
**問いが答えを1つに絞れていませんでした。**

そして **この2つは「最も小さい」のかかる先が逆**です。ここを間違えると、
**問いが誤答を指すのに、検査は通ってしまいます。**

| | 正解 | 条件を満たす他 | 正解のマスクは | だから |
|---|---|---|---|---|
| 7 台数 → マスク | `/25` 255.255.255.128 | `/24` `/23`（もっと入る） | いちばん**大きい** | 小さいのは**サブネット**のほう |
| 8 個数 → マスク | `/19` 255.255.224.0 | `/20` `/21`（もっと作れる） | いちばん**小さい** | **サブネットマスク**でよい |

- 7 → 「この台数が入る、最も小さい**サブネットの**サブネットマスクはどれですか。」
- 8 → 「このサブネット数が作れる、最も小さい**サブネットマスク**はどれですか。」

`build.js` が 800問ぶん、**「最も小さい」の向きが答えと合っているか**と、
**かかる先の言い方**（7は「最も小さいサブネットの」、8は「最も小さいサブネットマスク」）を見ます。

ステージ9と10には**同じ問題**（アドレスをまとめる）が出るのに、言い方が違っていました。
10のほうにそろえています。

`build.js` が **「問いの言葉が宙に浮いていないか」**を見ます
（`。` のあとに `どれ` `どこ` `いくつ` `何` だけが残る形を止める）。

### 仕上げ（ステージ10）だけ、練習を置かない

仕上げには**新しいやり方がありません**（`WAY.SF`「新しいやり方は出てきません」）。
それなのに「やってみる」も「練習をする」もあって、**どちらもテストと同じ問題を出していました。**

```
札を押す → [ はじめる ] → 説明の1枚（本番で聞かれる8つの形）→ [ テストをする ]
```

の一本道にしました。`gen.js` の `drill: false` 1つで決まります。

- 説明の1枚から「やってみる」の段を外す（段は 前のステージから → 解き方 → 見本 の3つ）
- 下の入口は「テストをする」1つだけ。1つしか無いので緑のベタ（＝いま進む道）
- トップの札のボタンも1つだけ（「はじめる」）
- **テストの鍵を開ける条件を変えた。**前は「練習で 5問中4問」が条件だったので、
  練習が無いと `lit` にならず、**テストが永久に開かないところだった**

`node .check/smoke.js` が、札の状態と画面が食い違っていないかを見ます
（段の並び／下の入口の数と文字／「やってみる」の有無）。

**残っていること**：トップの合図は ○ まだ ／ ● 練習ができた ／ 🏅 バッジ の3段ですが、
仕上げには練習が無いので **● を飛ばして ○ から 🏅 へ**行きます。

### ステージ8の練習が、16マス中6マスしか答えにならなかった

必要なサブネット数が **2〜60 の13通り**しか無く、押す数は `2 / 4 / 8 / 16 / 32 / 64` の6通り。
盤には 2の0乗〜2の15乗 の**16マス**があるのに、**10マスは一生答えになりません**でした。

原因は、クラスを見ずに同じ数の並びから選んでいたこと。
**クラスによって、延ばせる桁数の上限が違います**（/30 より細かくは分けられない）。

| クラス | 出発点 | 延ばせる桁 | 作れるサブネット数 |
|---|---|---|---|
| C | /24 | 6桁 | 64 まで |
| B | /16 | 14桁 | 16384 まで |
| A | /8 | 15桁（盤の上限） | 32768 まで |

クラスに合わせて選ぶようにしたので、大きい数も安全に出せます。
押す数は **2 から 8192 まで、16マスのうち13マスに当たる**ようになりました
（残り3マスは、どのクラスでも /30 を超えるので出ません）。

**あわせて見つかった間違い**：盤の段のラベル「第3オクテット」「第4オクテット」は、
**クラスC のときしか合っていませんでした。**クラスB なら第3、クラスA なら第2 を指します。
台数のほう（ステージ7）は 32 から数えるので位置が決まりますが、
サブネット数のほうは出発点がクラスで変わります。段が何なのかは上の「2の◯乗」が
すでに言っているので、名前は出さないことにしました。

`.c-w2.sm`（32768 などの5桁）が、幅375pxでまだ 2px はみ出していたのも直しました。
11px が下限なので、字間だけ詰めています。

### トップ画面を、基礎 ／ 試験レベル ／ 仕上げ で区切った

10個の札がただ並んでいて、**どこからが本番の話か分かりませんでした。**

```
基礎        ステージ 1 〜 4    試験レベルの問題を解く土台になります。
試験レベル  ステージ 5 〜 9    本番で実際に問われる形の問題です。
仕上げ      ステージ 10        ここまでの全部を混ぜて、本番と同じ形で解きます。
```

区切りは `gen.js` の `GROUPS`。範囲（1〜4 など）は並びから出すので、
ステージを差し込んでも書き直しは要りません。
`build.js` が「どのまとまりにも入っていない札」を止めます。

**踏んだ落とし穴**：`.gsec:first-child` では効きません。札はどれも1枚ずつ包まれているので、
**どの区切りも「包みの先頭の子」**になります（`.road > div:first-child > .gsec` にした）。

### 「図で見ると」を、表（グリッド）で組み直した

前は**等幅フォントで中央ぞろえしていただけ**でした。だから、

- 名札の幅が行ごとに違う（「IP 135」と「全部 0」）と、**区切り位置が行ごとに左右へずれる**
- `255` が、自分のオクテットの**真下に来ない**

いまは表なので、列が機械的にそろいます。区切り位置は**行をまたいで1本の縦線**になり、
`255` は自分の `11111111` の真下に来ます。見出しも、区切りのある列でぴったり分かれます。

```
        ネットワーク部  │  ホスト部        ← 見出しも区切りの列で分かれる
IP 135    1  0  0  ┃ 0  0  1  1  1
全部 0    1  0  0  ┃ 0  0  0  0  0   …10.128
          いちばん小さい数 ＝ ネットワークアドレス
全部 1    1  0  0  ┃ 1  1  1  1  1   …10.159
          いちばん大きい数 ＝ ブロードキャストアドレス
```

**枠は付けません。**「枠があるもの＝押せるもの」という決まりがあるので、
マスの区切りは下地の色だけで見せています（区切り位置だけは、枠ではなく1本の線）。

ステージ4は、区切りが4つめのオクテットの**中**にあるので、見出しを列では分けられません。
左右に置くだけにして、正確なところは図の下の1行とマスの中の線にまかせています。

**踏んだ落とし穴を2つ**（同じことをやると、また静かにずれます）:

1. `.fg-r:empty{display:none}` ── 中身の無いマスを消したら、**そのマスが表から抜けて次の行が1列ずれた。**
   グリッドでは、空でも置いておく
2. 一部だけ「2列目から最後まで」と指定して残りを自動に任せたら、**自動の置き場所がそこで飛んだ。**
   いまは全部のマスが、自分の行と列を明示している

`build.js` が「列の数と、各行のマスの数が合っているか」を見ます。

### 「線」は、ステージ5では「線」のまま

ステージ5は、**8つのマスに実際に線を引く**操作なので「線」が自然です。機械的には変えません。
「区切り位置」と呼ぶのは、ステージ4の**定義の説明**のところだけです。

### 「中1の言葉で」を、「ひらがなで書く」と取り違えていた

`とちゅうから 0 になり` のように、**途中だけひらがな**になっていました。
同じ画面で「**引き**算」と書いておきながら「**たし**算」、
コメントで「見**比べ**る」と書いておきながら画面では「見**くらべ**て」。

決まりはこうです。**常用漢字は漢字で書く。中1が読めない字だけ ひらがな。**
（「そろえる」「ずらす」は常用漢字表に無いので、ひらがなのままでよい）

直した語（`gen.js` と `app.jsx` で83か所）:

| | |
|---|---|
| とちゅう→**途中** ／ ぜんぶ→**全部** ／ おなじ→**同じ** ／ つづく→**続く** | ならぶ→**並ぶ** ／ のばす→**延ばす** ／ ちがう→**違う** |
| はじめて→**初めて** ／ うしろ→**後ろ** ／ まちがえ→**間違え** | あまり→**余り** ／ のこり→**残り** ／ かぞえ→**数え** |
| たし算→**足し算** ／ 〜ぶん→**〜分** ／ ふくむ→**含む** | のぞいた→**除いた** ／ 見くらべ→**見比べ** ／ こんど→**今度** |

これらは `build.js` が止めます（`gen.js` の説明の表も見ています）。
なお **「ひっくり返す」だけは、そのまま**にしています（「引っ繰り返す」とは書かない）。

### 画面の言葉から、励ましと呼びかけを外した

3か所ありました。

| 前 | いま |
|---|---|
| まちがえても大丈夫。正解するまで、同じ問題をやり直します。 | 正解するまで、同じ問題をやり直します。 |
| …いちばん左の数を**押しましょう**。 | 押すのは、上の列で いちばん左の数**です**。 |
| バッジはもう持っています。**この速さを保ちましょう。** | バッジはもう持っています。 |

**何が起きるかを1つも増やさないのに、読む量だけ増える言葉**です。
「大丈夫」「ましょう」「がんば」「その調子」は `build.js` が止めます（`gen.js` の表も見ています）。

**言葉の見張りも、`gen.js` の表まで届いていませんでした。**
`app.jsx` の行しか見ていなかったので、説明文を表に移すと全部が見張りの外に出ます。
`WAY` `LINK` `FIGURE` `EXAMPLES`（題・行・注記）も見るようにしたところ、
`EXAMPLES.S7` に「6ビット目」が残っているのがすぐ出ました（「上から 6つ が同じ」に直した）。

---

## 11. まだ無いもの

**見た目の大きさは、実機で最終確認してください。**
幅375pxでマスの幅が約34pxになります（高さは44px）。押し間違えても線の位置がすぐ見え、
押し直せて、答えの下見も出るので実害は小さいはずですが、指で触った感触は確かめてください。

そのほか：サーバー（成績はブラウザの中だけ）／音／1日の目標や連続日数。

---

## 12. 引き継ぎ（次のセッションへ）

### いまの形

```
トップ画面（10ステージの札）
  └ 札を押す →「練習する」→ 説明の1枚（＝チュートリアル）
                              └ 下に固定：[ 練習をする ] [ テストをする ]
                                              ↓                ↓
                                        盤で5問（表あり）   表なしで10問 → 9問で 🏅
                                                            （仕上げは15問 → 14問）
```

### トップ画面の「変換を楽にするコツ」（`TipBlock` / `app.jsx`）

まとまり（基礎／試験レベル／仕上げ）の**最後の札の下**に、押すと開くブロックが1つ出ます。
いまあるのは基礎の最後（ステージ4の下）の1つだけです。中身は `gen.js` の `GROUPS` の `tip`。

```
  4  プレフィックス長 ↔ サブネットマスク   🏅
 ┌────────────────────────┐
 │ 2進数 → 10進数の変換を楽にするコツ  ＋ │  ← 押すと開く。最初は閉じている
 └────────────────────────┘
 ──────────────────────────
  試験レベル  ステージ 5 〜 9
```

**ここは解き方の代わりではありません。**教材の手順（1 にした桁の数を全部足す）で
解けるようになったうえで、よく出る並びを覚えておくと、その足し算を省ける、という位置づけです。
だから **解き方の帯にも、説明の1枚にも混ぜません。**まとまりを解き終えた場所に、閉じて置きます。

- 札ではないので、**ランプもバッジも置きません**（札と見分けがつかなくなる）
- 開いているかどうかは、右の印（＋ −）で言う。**最初は閉じている**（開いたままだと札の列が読めない）
- 増やすときは `GROUPS` に `tip` を足すだけ。画面側は触らない
- `node .check/loop.js` が、置き場所（まとまりの最後か）と、押すと開いて閉じるかを見ています

**説明の1枚は、10ステージすべて同じ骨組みです。**（`Memo` / `app.jsx`）

```
 ✕
 チュートリアル ・ ステージ 4 / 10     ← 13px 灰
 プレフィックス長 ↔ サブネットマスク    ← 22px 太字
 1問やって、解き方を覚えます           ← 13px 灰
 ━━━━━━━━━━━━━━━━  ← ここから中身

 前のステージから        ← 名札。gen.js の LINK（全10ステージ）
 ┃ 解き方              ← 赤い縦線。名札は付けない。gen.js の WAY
 図で見ると             ← 名札。gen.js の FIGURE（ステージ4・5だけ）
 見本                  ← 名札。gen.js の EXAMPLES（題・行・注記）
 やってみる             ← 名札。1問を最後まで手で解く（Tutorial）

 [ 練習をする ]  [ テストをする ]   ← 下に固定。上にぼかし
```

- **名札は4語だけ。**見た目は1種類（`.sec`）。下地も枠も縦線も付けない
- **目立つ帯は、赤い縦線の「解き方」1つだけ。**増やすと、どこが大事か言えなくなる
- 段を足したり順番を変えたりすると、**`node .check/smoke.js` が止まります**

**例外は仕上げ（ステージ10）だけ**です。新しいやり方が無いので `drill: false` にしてあり、
「やってみる」と「練習をする」を置きません（段は3つ、下の入口は「テストをする」1つ）。

ステージは10。1 2の◯乗／2 2進数→10進数／3 10進数→2進数／4 プレフィックス長↔サブネットマスク／
5 ネットワークアドレスとブロードキャストアドレス／6 使えるアドレスの範囲／7 台数→サブネットマスク／
8 サブネット数→サブネットマスク／9 まとめる（集約）／10 仕上げの演習

### 全体を貫く決まり（これを破らない）

1. **軸は教材PDF。**そこに「よく出るものを覚えさせる」を足したもの。**教材に無い近道の解き方は入れない**
2. **画面も・覚える中身も・進み方も、「いま思い出すこと」に効くものだけを、最小の形で置く**
3. **正解するまで、次に進まない。**練習でもテストでも同じ。点になるのは**最初の答えだけ**
4. **正解のマスを光らせる手引きは入れない／押せなくする跳ね返しも入れない**
5. **枠があるもの＝押せるもの。**押せないものは枠なしの平らな文字。**空きは破線**
6. **色だけに頼らない**（✓✕の形も出す）。青＝いま選んでいるもの／緑＝進む・できた／赤＝ちがう／黄＝線／灰＝まだ
7. **文字は7段だけ**（11/13/15/17/22/34/44）、**色は決めた30色だけ**、**余白は6きざみ**（4/8/12/16/22/32）。
   書いてよいのは `app.jsx` の CSS の先頭の `:root` の中だけ。
   その外に `font-size:◯px` か `#hex` を書くと `node build.js` が止まる
8. **かたまりの見せ方は1種類だけ**（`.sec` の名札）。**目立つ帯は1画面に1つだけ**（解き方の赤い縦線）。
   新しい下地・枠・縦線を思いついたら、まず「いまある名札で足りないか」を見る
9. **計算も、説明の文章も `gen.js` に置く。**画面側で計算しない／画面側に文章を直書きしない
   （`WAY` `LINK` `FIGURE` `EXAMPLES`。直書きすると、そのステージだけ骨組みが崩れる）
10. **本番の言葉は正確に**（サブネットマスク・プレフィックス長・ネットワークアドレス・ブロードキャストアドレス・ホスト部・オクテット）。
    それ以外は中学1年生の言葉。「ビット目」「基準」「かたまり」は `build.js` が止める
    （問いと手順だけでなく、`gen.js` の説明の表も見ています）
11. **「中1の言葉で」は「ひらがなで書く」ではない。常用漢字は漢字で書く。**
    中1が読めない字（そろえる・ずらす など常用漢字表に無いもの）だけ ひらがな。
    「とちゅう」「たし算」「見くらべ」のような中途半端なかなは `build.js` が止める
12. **画面の言葉は説明だけ。励ましも呼びかけも入れない。**
    「まちがえても大丈夫」「この速さを保ちましょう」は、何が起きるかを1つも増やさないのに読む量だけ増える。
    書くのは**何が起きるか**（「正解するまで、同じ問題をやり直します」）。
    「大丈夫」「ましょう」「がんば」「その調子」は `build.js` が止める
13. **原理から外れる実装をするときは、事前に「外れます」と伝えてから**

### ステージ4の②（サブネットマスク → プレフィックス長）

**答えの /◯ は、オクテットごとの 1 の数を足した数です。**それを画面に出します。

```
① 255 のオクテットを、左から順に押します
      255  .  255  .  255  .  240
1 の数  8  ＋   8  ＋   8  ＋   4     ← オクテットの真下。押した分だけ青く濃くなる

② 255 でないオクテットを、1 と 0 で作ります
      1 1 1 1 0 0 0 0
      128 ＋ 64 ＋ 32 ＋ 16 ＝ 240    ← 青い式。オクテットを作る計算

      1 の数を全部足すと 28            ← 灰の名前 ＋ 大きい数（.o-n）

 プレフィックス長      /28            ← 答え
 サブネットマスク      255.255.255.240
```

**合計を青い式（`.o-x`）で出さないこと。**②の「128 ＋ 64 … ＝ 240」と見分けが付かなくなります
（同じ見た目で別のことを言うのが、いちばん読みにくい）。足し算そのものは「1 の数」の行が
＋ 付きで見せているので、ここは合計だけを名前付きで出します。

**手順（`gen.js` の `steps`）も、問われた向きに分けてあります。**
前は toMask の手順しか無く、この向きでも「/28 なら 1 が 28個」から始めていました。
**答えの 28 から逆算していたので、28 がどこから出てきたのかを1つも言えていませんでした。**

計算は `onesPerOctet()`（`gen.js`）。255→8 240→4 0→0 を返し、足すとプレフィックス長そのもの。
検査は `node .check/masklen.js`（画面と同じ手つきで①②を押し、並び・合計・答えが揃うか。
余りのある回と無い回の両方を必ず通す）。

### ステージ5の作り（いちばん手を入れたところ。他へ広げる見本）

```
① サブネットマスクを左から見て、はじめて 255 でなくなる数を押す   ← 押す
② サブネットマスクの 252 を 1 と 0 にすると    ← 機械が出す。引き算の過程を見せる
③ 同じオクテットの IPアドレス 219 を 1 と 0 にすると  ← 同上
④ 上の 219 の並びで、線から右を ぜんぶ 0 にする   ← 行ごと1回押す
⑤ おなじ並びで、線から右を ぜんぶ 1 にする       ← 同上
   この8つ ＝ 112　うしろは ぜんぶ 0
   10.69.112.0
```

**計算はゼロ。押すのは3回。**ただし**過程は必ず見せる**（消すと「この並びはどこから来たのか」に戻る）。

### 確かめかた

```bash
cd /Users/e2304-01/ccna/ipcalc2
node build.js                    # ビルド ＋ 生成器 ＋ 盤 ＋ 文字と色 ＋ クラス名 ＋ 言葉
node selftest.js 60 > /tmp/q.jsonl && python3 selftest.py /tmp/q.jsonl   # Python で全件照合
open index.html
git add -A && git commit -m "…" && git push     # 1〜2分で公開先に反映
```

**`node build.js` を挟み忘れると、検査が古い `app.js` を見て嘘の結果を出す。**

### ステージ5のテスト（補助の入れ方の見本）

```
問1〜5    手順テスト（次に何をするかを選ぶ → 選んだ処理だけをやる → … → 最後は4択）
問6〜10   本番形（メモの8マス ＋ 4択だけ。促しなし）
合格は 9/10。途中で1回でも外したら、その問題は ✕
```

**補助の境目（1文）：**
> **「手」の代わり（書く場所と、決め終えた足し引きの実行）までは出してよい。
> 「目と判断」の代わり（どこを見るか・どこに線か・どれを1にするか、を先に示すもの）は出さない。**

根拠：**本番の試験会場では、書いて消せるボードが渡される。**「白い書く場所」は本番にもある補助。
だから**メモの8マスは出すが、重みは書かない**（思い出す仕事を機械が先にやってしまう）。

- 手順の外れは「**別のステージのレシピとの混同**」が主力（ワイルドカードの手、次ステージの ＋1 など）。
  本番は「これはステージ5だ」と教えてくれない。**レシピの選別こそ本番の力**
- **成績や時間で補助を変えない**（物差しが問題ごとに変わると10問の点に意味がなくなる）。
  位置で決まる固定の段だけにする
- 全問を手順テストにしない ── **選ぶのは、思い出すより必ず易しい。**
  全問そうすると 🏅 が「メニューがあればできる」の証明に薄まる

手順データは `gen.js` の `stepRounds()`。画面は `TestBoard` の `q.steps5` の枝。
検査は `node .check/steptest.js`（手順どおりで正解になる／途中で外すと ✕ になる）。

### 検査は7本

```bash
node build.js            # ビルド ＋ 生成器 ＋ 盤 ＋ 見た目 ＋ クラス名 ＋ 言葉
node .check/loop.js      # 一周する道すじ（鍵 → 練習で ● → 次が開く → テストで 🏅 → 外すと ✕）
                         #   ＋ トップ画面の「変換を楽にするコツ」が押すと開くか
node .check/smoke.js     # 10ステージの盤が描けるか ＋ 段の骨組みが10ステージで同じか
node .check/split5.js    # ステージ5・6を12問、①〜⑤の順に解いて正解になるか
node .check/steptest.js  # ステージ5のテスト（手順テスト）が通るか
node .check/table.js     # ステージ1の「表を8つともうめる」が通るか
node .check/final.js     # 仕上げのテスト（15問・8つの型・合格ライン）
node .check/masklen.js   # ステージ4の②：答えの /◯ が、1 の数の足し算から出ているか
node selftest.js 100 > /tmp/q.jsonl && python3 selftest.py /tmp/q.jsonl
```

もとは `.check/run.js` 1本だったが、**もう無い画面（暗記ドリル）を前提に書かれていて
落ちたまま**になっていた。409行あって、直すより書き直すほうが早い状態だった。
削除して、道すじの検査だけを `loop.js`（130行）に書き直してある。
**落ちたままの検査は、無いより悪い。**「検査は通っている」と思い込む材料になる。

### 残っている宿題

1. **余白が刻みの外に77か所。**いまは警告だけ（`node build.js` の最後に出る）。
   1〜7px（31か所）は マスの中の微調整、72px・110px は固定の帯に合わせた寸法なので、
   **刻みに寄せるべきなのは 9 / 10 / 14 / 18 / 20px の42か所**
   （9:3 ／ 10:16 ／ 14:16 ／ 18:4 ／ 20:3。11px の2か所も残る）。
   ここを直すと全画面の間の取り方が変わるので、まとめてやること
2. **ステージ6〜10 に、ステージ5の考え方を広げる**（計算は機械に・過程は見せる／段どうしを言葉と色でつなぐ）

### この会話でやらかしたこと（同じ失敗を避けるために）

- **大きな範囲を一度に置き換えて、ファイルを2回壊した。**削除も置換も**1つずつ**に分ける
- **消してはいけないものを2回巻き込んで消した**（`EXAMPLES` など）。範囲を広く取らない
- **指示を字面で受け取り、勝手に別の仕組みに作り替えた。**原理から外れるときは**先に言う**
- **検査が画面と同じ道すじを通っていなかったので、`/24` より短い問題が永久に解けないバグを見逃した。**
  検査は**画面が通る道と同じ道**を通すこと
